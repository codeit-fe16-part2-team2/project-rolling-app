name: Add issue to Project v2

on:
  issues:
    types: [opened]

jobs:
  add-to-project:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # ✅ 여기 아래에 추가하면 됩니다
      - name: Check GH_TOKEN exists
        run: echo "GH_TOKEN is ${{ secrets.GH_TOKEN != '' && 'set' || 'missing' }}"

      - name: Add issue to Project
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GH_TOKEN }}
          script: |
            // your GraphQL logic
